version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    container_name: chess-backend

  frontend:
    build: ./frontend
    ports:
      - "3001:3001"
    container_name: chess-frontend
    depends_on:
      - backend
